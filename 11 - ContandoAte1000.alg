algoritmo "Contandoate1000"
var
    Contador: Inteiro
inicio
      Contador <- 0
      Enquanto (Contador <= 1000) faca
      Escreval (Contador)
      Contador <- Contador + 5
      FimEnquanto
fimalgoritmo
