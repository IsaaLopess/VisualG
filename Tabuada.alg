algoritmo "Tabuada"
var
   contador, N: Inteiro
inicio
      contador <- 1
      Escreva ("Qual casa da tabuada voce quer?: ")
      Leia (N)
        Repita
              Escreval (N, " x ", contador, "=", n * contador)
              contador <- contador + 1
        Ate (contador > 10)
fimalgoritmo
